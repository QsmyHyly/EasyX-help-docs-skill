# moverel

这个函数用于移动当前点。 有些绘图操作会从“当前点”开始，这个函数可以设置该点。还可以用 [moveto](moveto.htm) 设置当前点。
    
    
    void moverel(
    	int dx,
    	int dy
    );
    

## 参数

### dx

将当前点沿 x 轴移动 dx。

### dy

将当前点沿 y 轴移动 dy。

## 返回值

无

## 备注

该函数已废弃，仅在 graphics.h 中声明，不推荐使用。

## 示例

无
